# This is a basic workflow to help you get started with Actions

name: REact app deployed to surge

# Controls when the action will run. Triggers the workflow on push or pull request
# events but only for the master branch
on:
  push:
    branches: [ master ]


# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  build:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
    # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
    - uses: actions/checkout@v2
    
    - name: Install NodeJS
      uses: actions/setup-node@v2-beta
      with:
        node-version: 12
        
    - uses: borales/actions-yarn@v2.0.0
      with:
        cmd: install
        
    - name: Build React App
      run: yarn build
      
    
      
    - name: install chart
      run: npm install chart-reactjs-2
      
    - name: install material ui
      run: npm install @material-ui/core
      
    - name: install class names
      run: npm install classnames
      
      
    - name: install chart
      run: npm install chart-js
      
    - name: install axios
      run: npm install axios
      
    # Install Surge
    - name: Install Surge
      run: npm install --global surge
    
    # Deploy Surge
    - name: Deploy React App to Surge
      run: surge ./build covid19trackerwm.surge.sh --token ${{ secrets.SURGE_TOKEN }}
